# Bağımlılıklar / derleme çıktıları
node_modules
dist
coverage

# Rust / Tauri üretilmiş + derleme çıktıları
src-tauri/target
src-tauri/gen

# Eski Node backend/agent (port bitene kadar referans) ve Python listener
legacy
eventlistener

# Lockfile'lar (elle biçimlendirilmez)
package-lock.json

# Değişmez/otomatik dosyalar
*.min.*
CHANGELOG.md
screenshots
